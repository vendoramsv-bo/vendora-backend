{
  "name": "vendora",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "db:seed": "tsx prisma/seed.ts",
    "db:reset": "prisma migrate reset",
    "db:validate": "prisma validate",
    "db:format": "prisma format"
  },
  "dependencies": {
    "@prisma/client": "^7.0.0",
    "better-auth": "^1.4.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "prisma": "^7.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "_comment_prisma_config": "La configuración de Prisma vive en prisma.config.ts (recomendado desde Prisma 7). NO se necesita sección 'prisma' aquí."
}